name: My first Workflow

on:
  pull_request:
    branches:
      - main   

jobs:
  pr-check:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "PR is created successfully!"

      - name: Print name
        run: echo "Rashi"
